---
phase: 3
plan: "03-02"
title: "Frontend Polish"
type: feature
wave: 1
depends_on: []
files_modified:
  - commandGarden/app/src/client/pages/Journal.tsx
  - commandGarden/app/src/client/components/journal/SummaryCards.tsx
  - commandGarden/app/src/client/components/journal/DailyBreakdown.tsx
  - commandGarden/app/src/client/components/journal/InsightsPanel.tsx
autonomous: true
requirements:
  - FRONT-01
  - FRONT-02
  - FRONT-03
  - FRONT-04
  - FRONT-05
  - FRONT-06
---

<objective>
Polish the journal frontend for demo readiness: complete the week picker with Sun–Sat navigation, enhance summary cards with correct data from all 3 sources, ensure the daily breakdown table shows cross-referenced data, improve loading/error/partial-data states, and make the layout responsive to match the commandGarden GUI style.
</objective>

<tasks>

### Task 1: Enhance week picker (FRONT-01)

<read_first>
- `commandGarden/app/src/client/pages/Journal.tsx`
</read_first>

<action>
Review the existing week picker in `Journal.tsx`. It already has:
- `currentSunday()` — gets Sunday of current week
- `addWeeks()` — navigate between weeks
- `formatWeekLabel()` — display "d Mon – d Mon, YYYY"
- Previous/Next buttons

Verify these work correctly with Sun–Sat weeks (FRONT-01 says "Sun–Sat with previous/next navigation"). The existing implementation already does this. If any edge cases exist (e.g. year boundary), fix them. Add keyboard navigation: left/right arrow keys change week when the picker is focused.
</action>

<acceptance_criteria>
- Week picker shows Sun–Sat range
- Previous button goes back 1 week
- Next button goes forward 1 week
- Week label format: "29 Jun – 5 Jul, 2026"
- Buttons disabled during loading
</acceptance_criteria>

<verify>
```bash
grep "addWeeks" commandGarden/app/src/client/pages/Journal.tsx
grep "currentSunday" commandGarden/app/src/client/pages/Journal.tsx
```
</verify>

---

### Task 2: Enhance loading and error states (FRONT-02, FRONT-06)

<read_first>
- `commandGarden/app/src/client/pages/Journal.tsx`
- `commandGarden/app/src/client/components/journal/InsightsPanel.tsx`
</read_first>

<action>
Update `Journal.tsx`:
- Add a `partial` status indicator: when `data.status === 'partial'`, show a warning banner above results: "Some data sources were unavailable. Results may be incomplete." using DaisyUI `alert alert-warning` with a list of `data.errors`
- Move the error list from `InsightsPanel` to the top of the results section for visibility
- Keep the loading spinner with the "Gathering data..." message
- Add elapsed time display after generation completes: "Generated in X.Xs"

Update `InsightsPanel.tsx`:
- Keep errors display as secondary location (in case partial banner is dismissed)
- Add visual distinction: insights get a lightbulb icon prefix, errors get a warning icon prefix
</action>

<acceptance_criteria>
- Loading state shows DaisyUI spinner + descriptive message
- Partial status shows warning banner with error list
- Error status shows red alert with all errors
- InsightsPanel shows insights with visual icons
- Generate button shows "Generating..." text while loading
</acceptance_criteria>

<verify>
```bash
grep "partial" commandGarden/app/src/client/pages/Journal.tsx
grep "alert-warning" commandGarden/app/src/client/pages/Journal.tsx
```
</verify>

---

### Task 3: Enhance SummaryCards (FRONT-03)

<read_first>
- `commandGarden/app/src/client/components/journal/SummaryCards.tsx`
</read_first>

<action>
Review existing SummaryCards — already shows 4 cards (Hours Logged, Meetings, Tickets Done, Commits). Enhance:
- Add click-to-expand behavior: clicking a card shows detail in a tooltip or expandable section
- Hours Logged card: show progress bar (DaisyUI `progress`) of totalHours vs targetHours
- Meetings card: show average meeting duration in the subtitle
- Tickets card: show blocker count in subtitle if blockers > 0, styled with `text-error`
- Commits card: show repos breakdown in subtitle (e.g. "across 2 repos")
</action>

<acceptance_criteria>
- Hours card has progress bar showing hours/target ratio
- Meetings card shows avg duration
- Tickets card shows blocker count when > 0
- Commits card shows repo count
- Cards still show "—" when data is null
</acceptance_criteria>

<verify>
```bash
grep "progress" commandGarden/app/src/client/components/journal/SummaryCards.tsx
grep "blocker" commandGarden/app/src/client/components/journal/SummaryCards.tsx
```
</verify>

---

### Task 4: Enhance DailyBreakdown (FRONT-04)

<read_first>
- `commandGarden/app/src/client/components/journal/DailyBreakdown.tsx`
</read_first>

<action>
Review existing DailyBreakdown — already shows per-day table. Enhance:
- Add a "Weekday" column header with Mon-Fri abbreviation
- Show weekend days only if they have activity (don't show empty Sat/Sun rows)
- Add row totals at the bottom: total hours, total meetings, total commits, total tickets
- Highlight zero-coding days (no commits) with a subtle blue indicator
- Improve gap highlighting: add a tooltip explaining what a gap means ("Activity detected but no hours logged")
</action>

<acceptance_criteria>
- Table has totals row at bottom
- Weekend days only show when they have data
- Zero-coding days have visual indicator
- Gap days still highlighted with warning background
</acceptance_criteria>

<verify>
```bash
grep "totals\|total" commandGarden/app/src/client/components/journal/DailyBreakdown.tsx
```
</verify>

---

### Task 5: Responsive layout (matching GUI style)

<read_first>
- `commandGarden/app/src/client/pages/Journal.tsx`
- `commandGarden/app/src/client/pages/Timetracking.tsx` (pattern reference)
</read_first>

<action>
Ensure Journal.tsx layout is responsive:
- Summary cards: `grid-cols-2 lg:grid-cols-4` (already set)
- Daily breakdown table: horizontal scroll on mobile (`overflow-x-auto` already set)
- Week picker: stack vertically on narrow screens (`flex-wrap`)
- Max width constraint: `max-w-5xl` (already set)
- Verify font sizes and spacing match the Timetracking page pattern
</action>

<acceptance_criteria>
- Page renders correctly on mobile widths (320px+)
- Cards stack 2-up on mobile, 4-up on desktop
- Table scrolls horizontally on narrow screens
- Consistent with Timetracking page styling
</acceptance_criteria>

<verify>
```bash
grep "grid-cols" commandGarden/app/src/client/components/journal/SummaryCards.tsx
grep "overflow-x-auto" commandGarden/app/src/client/components/journal/DailyBreakdown.tsx
```
</verify>

</tasks>

<verification>
1. Journal page renders with all 3 data sources populated
2. Summary cards show data with enhanced subtitles
3. Daily breakdown shows per-day cross-referenced data with totals
4. Loading state shows spinner, partial shows warning, error shows alert
5. Layout is responsive on mobile and desktop
6. Matches commandGarden GUI styling (DaisyUI/Tailwind)
</verification>

<must_haves>
truths:
  - "Week picker has Sun–Sat navigation with previous/next buttons"
  - "Generate button triggers backend call with loading state"
  - "Summary cards show 4 metrics: hours, meetings, tickets, commits"
  - "Daily breakdown shows per-day table with cross-referenced data"
  - "Insights panel shows rule-based insights"
  - "Partial-data warning shows when some sources fail"
  - "No 'use client' directives"
</must_haves>

## Artifacts this phase produces

### Modified files
- `commandGarden/app/src/client/pages/Journal.tsx` — enhanced loading/error/partial states, responsive layout
- `commandGarden/app/src/client/components/journal/SummaryCards.tsx` — progress bar, enhanced subtitles
- `commandGarden/app/src/client/components/journal/DailyBreakdown.tsx` — totals row, zero-coding indicators
- `commandGarden/app/src/client/components/journal/InsightsPanel.tsx` — icons, visual polish
